# dsh-jev 只需要一个密钥：所选后端对应的凭据引用。
#
# 复制为 .env 后填入真实值（.env 已在 .gitignore 中）。
# 也可以把值交给 dsh 的 credentials 服务管理，而不是放在这里。

# backend=openrouter（默认）
OPENROUTER_API_KEY=

# backend=typesafe
TYPESAFE_API_KEY=
